# Valores por defecto empaquetados (sin secretos). Sobrescribir con --dart-define o .env local.
# Default = desarrollo (apidev). Producción: usar .env.production / --dart-define-from-file.
API_BASE_URL=https://dashcampay.com/apidev
GOOGLE_MAPS_API_KEY=
NETPAY_PUBLIC_API_KEY=
